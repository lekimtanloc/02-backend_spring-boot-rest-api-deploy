apiVersion: v1
kind: Secret
metadata:
  name: {{ include "02-backend-spring-boot-rest-api.fullname" . }}-secret
type: Opaque
stringData:
{{- range $key, $value := .Values.secret }}
  {{ $key }}: "{{ $value }}"
{{- end }}
